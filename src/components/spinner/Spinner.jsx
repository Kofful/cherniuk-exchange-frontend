import Loader from "react-loader-spinner";

const Spinner = () => {
    return (
        <Loader type="BallTriangle"
                color="#6600CC"
                height={100}
                width={100}/>
    );
};

export default Spinner;
